
@model IEnumerable<Book>

<div class="col-lg-12">
    <div class="iq-card-transparent iq-card-block iq-card-stretch iq-card-height rounded">
        <div class="newrealease-contens">
            @foreach (var book in Model)
            {


                <ul id="newrealease-slider" class="list-inline p-0 m-0 d-flex align-items-center">
                    <li class="item">
                        <a href="javascript:void(0);">
                            <img src="@book.BookImage" class="img-fluid w-100 rounded" alt="">
                           <h2>@book.BookName</h2>
                        </a>
                    </li>



                </ul>
            }
        </div>
    </div>
</div>


@*<table class="table table-bordered">
    <tr>
        <th>ID</th>
        <th>Kategori</th>

    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.BookId</td>
            <td>@item.BookName</td>
  
        </tr>
    }
</table>*@